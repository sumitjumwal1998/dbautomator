- name: List of users/roles
  hosts: host1

  vars:
    db_name: postgres
  tasks:
    - name: run sql
      postgresql_query:
        db: "{{ db_name }}"
        query: SELECT rolname FROM pg_roles
      register: sql_data
    - name: print
      ansible.builtin.debug:
        var: sql_data
